{
  "name": "bigbang-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create bigbang-db",
    "d1:init": "wrangler d1 execute bigbang-db --file=./schema.sql",
    "d1:init-local": "wrangler d1 execute bigbang-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
